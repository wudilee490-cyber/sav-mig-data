"""sav_mig_data — Convert SA-V (or similar) datasets into MIG training cache.

This package is intentionally INDEPENDENT from the vace_mig model project.
The contract between the two projects is the on-disk cache format
(see README.md for the exact directory layout).
"""
__version__ = "0.1.0"
